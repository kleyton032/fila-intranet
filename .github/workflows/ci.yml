name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  # ─── Backend ────────────────────────────────────────────────────────────────
  backend-ci:
    name: Backend – Lint, Type-check e Testes
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Instalar dependências
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Testes unitários
        run: npm run test:cov

      - name: Upload cobertura
        uses: codecov/codecov-action@v4
        with:
          directory: backend/coverage

  # ─── Build de produção (apenas main) ────────────────────────────────────────
  build:
    name: Build de produção
    needs: [backend-ci]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - uses: actions/checkout@v4

      - name: Build das imagens Docker
        run: docker compose build

      - name: (Opcional) Push para registry
        # Descomente e configure quando tiver um registry Docker configurado
        # run: |
        #   echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
        #   docker compose push
        run: echo "Etapa de push desabilitada — configure o registry"
