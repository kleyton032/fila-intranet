import React from 'react';

const LabelInput = ({title}) => {
        return (
            <div>
                <label className="label-title">{title}</label>
            </div>
            
        );
        
};

export default LabelInput;

